package com.tutorial.uprit.model;

/**
 * Enum representing the type of achievement a user can post.
 * Each type maps to a specific XP reward in the rules engine.
 */
public enum AchievementType {
    HACKATHON_WIN,
    HACKATHON_PARTICIPATION,
    CERTIFICATE,
    PROJECT_COMPLETION,
    INTERNSHIP,
    RESEARCH_PAPER,
    OTHER
}
